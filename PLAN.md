# 낙장도메인 품질 체커 — 실행 계획서 (v6)

> 작성일: 2026-08-04 · 웹조사 확인일: 2026-08-04 · 저장소: git@github.com:harisbaguette/domainhistorychecker.git (연결·푸시 완료)
> 이력: v4 → 심판1(계획 가정 반박) 결함 30건 반영 = v5 → 심판2(품질 게이트) 치명 3·중 8 반영 = v6
> 용어 통일: "낙장도메인"(만료 도메인), "승계 자산"(물려받는 백링크·권위), "말기"(마지막 활동 2년 구간)

## 목적 (모든 설계는 여기서 역산)

**"구글 상위노출용 사이트를 운영할, 안전하고 유리한 낙장도메인을 대량 후보 중에서 골라낸다."**

| 판단 질문 | 필요한 증거 | 수단 |
|---|---|---|
| Q1. **위험한가?** | 전 기간 과거 콘텐츠의 스팸성 운영, 블랙리스트, 색인 오염 | Wayback 전 기간 + AI 정독 + 색인 제목 + Spamhaus (·Safe Browsing 선택) |
| Q2. **건실한가?** | 운영 연수, 이력 흐름(전환 방향), 리다이렉트·파킹 비중 | Wayback CDX + RDAP/whois + AI 주제 변천 |
| Q3. **승계 자산이 있는가?** | 권위 점수, 색인 잔존 | Open PageRank + Serper + (상세는 Ahrefs 무료 링크) |
| Q4. **뭘 하면 유리한가?** | 과거 주제·언어·독자층 → 이어갈 주제 | AI 추천 (구글 정책상 "과거 인접 주제 + 사람 우선"이 안전) |
| Q5. **지금 살 수 있나? 법적 문제는?** | 등록 상태, 상표 충돌 가능성 | RDAP status + 브랜드명 검사 + AI 소견 |

**검사의 두 집합 (심판2 치명-1 반영)**:
- **필수 검사** (✅ 발급의 전제 — 하나라도 미확인이면 최고 ⚠️): Wayback 이력 판독(또는 "이력 없음" 확정), 등록 정보, Spamhaus, 색인 검사(Serper 키 필요 — 키 없으면 ✅ 불가를 UI에 명시), AI 분석, 권위 점수
- **선택 검사** (없어도 ✅ 가능, 보고서에 "미실시" 각주만): Safe Browsing, VirusTotal
- 원칙 유지: "증거 없음"≠"문제 없음". 필수 검사 실패는 미확인으로 표시.

## 참고: 구글 정책 (2026-08-04 조사)
- 공식 스팸 정책 "만료도메인 남용" 항목(2024-03 도입, 2026-05-15 갱신 문서에서 유효 확인): 금지 = 순위 조작 목적 저가치 콘텐츠, 허용 = *"a new, original site that's designed to serve people first."* 적발 시 순위 하락·색인 제외.
- 업계 통설: 순위 승계 없음, 자산은 백링크, 안전 조건 = 과거 인접 주제.
- 출처: developers.google.com/search/docs/essentials/spam-policies · name.com/blog · pbn.ltd · domraider.com

## 1. 파이프라인

```
브라우저 UI (입력: 줄바꿈·쉼표 구분 붙여넣기 또는 .txt/.csv 업로드
             → www·프로토콜 정규화, 중복 제거, 상한 1,000개 — 초과 시 분할 안내
             → 시작 전 예상 소요·쿼터 소진 표시)
   │
   ├─[A] 이력 수집 — CDX 1회(연도 분포·공백·상태코드·차단(exclusion) 응답 구분)
   │      + 연도별 대표 스냅샷 본문 최대 6개(말기 필수), `id_` 원본, 본문 추출 후 스냅샷당 6천자
   ├─[B] 등록 정보 — RDAP → 실패 시 whois 폴백(.kr=KRNIC, EUC-KR 자동 감지)
   │      등록일·만료일·취득 상태(삭제 대기/복원 기간/이전 잠금/등록 중), 재등록(드랍) 이력 표시
   ├─[C] 평판 — Spamhaus DBL(필수, 시스템 리졸버, 차단 시 미확인) · Safe Browsing(선택) · VT(선택)
   ├─[D] 색인 — Serper site: 1쿼리: 색인 수+상위 제목. 오염 판단 전에 "현재 파킹/재활용
   │      콘텐츠"를 과거 이력과 구분(현재 상태를 과거 탓으로 돌리는 오탐 방지)
   ├─[E] 콘텐츠 분석 — 축을 통일한 2갈래 (심판2 치명-3 반영):
   │      · 규칙(운영방식 축): 도어웨이 흔적(유사 페이지 대량·키워드 나열), 숨김 텍스트/링크
   │        (배경색 글자·hidden 스타일), 외부 링크 무더기, 자동생성 반복 패턴, 파킹 패턴
   │      · AI(운영방식 축): deepseek-v4-flash-0731 1회, JSON 스키마 — 주제 변천, 스팸성 운영
   │        판정(+확신도 0~1 +원문 인용 근거), 전환 방향, 상표 충돌 소견, 추천 주제(한국어)
   │      · 업종 키워드(도박·성인·약품 4개국어 최소셋)는 판정 축이 아니라 **주의 표지**:
   │        AI에게 힌트로 전달 + 보고서 표시만 (합법 업종 오폭 방지)
   │      · 도메인명 브랜드 문자열 검사 → 걸리면 ⚠️ 사유
   ├─[F] 캡쳐 — Playwright: 말기 1장 + 위험 신호 시기 1장(있을 때만). Wayback 예산에 계상,
   │      전 검사 완료 후 후행 실행(표 먼저, 사진 나중)
   ▼
[G] 점수·판정·보고서 (코드)
```

- 캐시: 도메인별 결과 JSON, 중단·재개 버튼. AI 입력 상한: 도메인당 ≈4만자(≈1.5만 토큰).

## 2. 점수·판정 (심판2 치명-1·2 반영)

**점수 (0~100)** — 현재 평판은 변별력이 없어(등재=치명 신호로 즉시 처리) 점수에서 제외하고 재배분:

| 항목 | 배점 | 채점 방향 |
|---|---|---|
| 과거 콘텐츠 안전성 | 45 | AI 스팸성 판정(확신도 가중) + 운영방식 규칙 + 색인 오염의 교차 결과 |
| 이력 건실성 | 20 | 연수·리다이렉트 남발·파킹 비중. 공백 단독 소폭, **공백+직후 주제 급변 조합**만 위험 |
| 전환 방향 | 15 | 정상→정상 소폭, 정상→위험 업종·언어 급변 대폭 감점 |
| 승계 자산 | 20 | OPR 권위 + 색인 잔존 **가점형** (색인 0은 만료의 기본값 — 중립) |

**판정 — 우선순위 순서대로 첫 해당 규칙 적용**:

1. **치명 → ❌**: 블랙리스트·Safe Browsing 등재(기관 확인) / 스팸성 운영 확정 = (a) 운영방식 규칙+AI 일치 또는 (b) AI 단독 고신뢰(확신도 ≥0.85 + 원문 인용 근거 필수) / 색인 오염(현재 파킹 제외 후)
2. **점수 <50 → ❌**
3. **⚠️ 강제**: 필수 검사 미확인 / 신호 충돌(규칙·AI 불일치) / 상표 충돌 의심 / 합법 민감 업종 이력 / 스냅샷 차단(이력 은폐 가능성) / **이력 없음 확정**(별도 표시 "이력 없음(신중)" — 점수는 수집된 항목만의 부분 점수로 참고 표기)
4. **50~74 → ⚠️ / 75+ → ✅**
- **⚠️ 비율 관리 (대량 선별 목적 보호)**: 시험지(§6-0) 기준 ⚠️가 전체의 30%를 넘으면 임계·규칙을 보정 — ⚠️로 다 쏠리면 선별 도구가 아니게 됨.
- 별도 열: 취득 상태(구매 가능/복원·경매 기간/등록 중). 컷라인 75/50·배점은 초기값, §6 시험지로 실측 보정.

**보고서 (HTML)**: `index.html` 판정별+점수순(도메인/판정/점수/취득 상태/한줄평/추천 주제) → 상세: 나이·재등록, 타임라인, 주제 변천, 캡쳐, 신호+근거 인용, 미확인·미실시 목록, 원자료, 재확인 링크(구글 site:·Ahrefs 무료·Moz DA·Whoisology·상표 검색). `results.json` 동봉.

## 3. 기술 스택 + 설치·보안 설계 (심판2 중 결함 반영)

**Python 3.12 + FastAPI + 반응형 단일 HTML UI + httpx(비동기) + Playwright + uv**
- 근거: 전부 윈도우+맥 공식 지원 / 브라우저가 UI / Tauri 탈락(모바일 Python sidecar 미지원, 공식 문서·GitHub #9774)

**UI 화면 3개 (미니멀의 실체)**:
1. **검사 화면**: 입력창(붙여넣기/파일) + 시작·중단·재개 버튼 + 진행률 바(SSE) + 결과 표(완료분부터 실시간 추가)
2. **상세 페이지**: 도메인 1개의 근거 전체(§2 보고서 상세와 동일)
3. **설정 화면**: API 키 입력(발급 안내 링크·그림 포함), AI 모델 선택, 속도 모드, 옵션 검사 켬/끔
- 디자인 원칙: 시스템 폰트, 단색+판정색 3개(✅초록·⚠️노랑·❌빨강), 표 중심, 장식 0

**키 저장·네트워크 보안**:
- 키는 사용자 홈의 앱 설정 파일(`~/.domainchecker/config.json`, 평문 — 로컬 1인 도구 범위)에 저장, 저장소엔 `.gitignore`로 제외
- 서버는 기본 **localhost 전용 바인딩** — 외부에서 접속 불가. 모바일 등 외부 접속 모드를 켤 때만 접속 비밀번호 설정을 강제 (키 노출 방지)

**설치 동선 (비개발자)**:
1. zip 다운로드 → 풀기 → 맥 `시작.command` / 윈도 `시작.bat` 실행
2. **최초 실행 차단 대응을 안내서에 그림으로 포함**: 맥 Gatekeeper(우클릭→열기 또는 시스템 설정 허용), 윈도 SmartScreen("추가 정보"→"실행") — 서명 없는 스크립트의 정상 경고임을 설명. (코드 서명은 유료·향후 검토)
3. 스크립트가 uv·의존성·Playwright 브라우저(수백 MB, 최초 1회) 자동 설치 후 브라우저 열어 줌
4. 키 없이도 실행 가능 — 단 필수 검사(Serper) 키가 없으면 ✅ 판정이 안 나옴을 설정 화면에 명시
- AI 모델 폴백 체인(3단 고정): `deepseek-v4-flash-0731` → `deepseek-v4-flash-latest` → `deepseek-v3.2` (폴백 시 단가 변동은 보고서 각주에 자동 표기)

⚠️ **보안 — 지금 즉시**: 채팅에 노출된 OpenRouter 키는 **바로 폐기·재발급** 후 새 키를 설정 화면에 입력할 것.

## 4. 속도 설계 (심판2 중 결함 반영 — 관측 하한 아래로)

- 병목은 Wayback IP 제한(관측치 분당 15~60건, 429 연타 시 1시간 차단·재위반 2배. 창 여러 개=같은 IP라 무효)
- **적응형 레이트**: 시작 분당 30건 → 429 첫 감지 시 즉시 **분당 12건(관측 하한 15 미만)**으로 하향 + 지수 백오프. 안전 우선 모드(처음부터 12건)도 설정 제공
- 도메인당 Wayback 예산: CDX 1 + 본문 ≤6 + 캡쳐 ≤2 ≈ 9건 → **100개 기준: 순조로우면(분당 30 유지) 약 30분, 안전 모드·하향 시 약 75~90분.** 표 결과(캡쳐 제외 ≈7건)는 그보다 약 20% 먼저 완성 — 시작 화면에 예상치 표시
- 다른 검사·AI는 Wayback 대기 그늘에서 병렬 완료. VT(옵션, 분당 4건)는 별도 줄
- (2차 확장) Common Crawl 본문 보충 — 커버리지 불확실(대형 사이트 편향)해 MVP 제외, 실측 후 판단

## 5. 비용표 (100개 기준, 2026-08-04 확인)

| 항목 | 비용 | 산정 근거 |
|---|---|---|
| AI (0731 기준 입력 $0.09/출력 $0.18 per 1M) | ≈ **$0.15~0.5** | 1.5만 입력+1천 출력 토큰 × 100 ≈ $0.15, 재시도·여유 포함 상한 $0.5. 폴백 모델 사용 시 변동(v3.2 입력 $0.21) |
| Wayback·RDAP/whois·Spamhaus | 무료 | — |
| Serper | 무료 2,500쿼리 내 | 도메인당 1쿼리 + 재시도 예비 ~10% → 실효 ~2,200개 |
| Open PageRank | 무료 | 월 3만, 100개 일괄 |
| Safe Browsing·VirusTotal (선택) | 무료 | VT 500/일·4/분 |

## 6. 구현 순서 (검증 게이트 포함 · 저장소는 연결 완료 상태에서 시작)

0. **시험지(정답 라벨 세트) 구축** — **조달 방법(심판2 지적 반영)**: ① 위험 라벨: 공개 위협 DB 아카이브(URLhaus·PhishTank 과거 등재분)와 SEO 커뮤니티에 공개된 스팸 사례 중 **현재 만료 상태인 도메인**을 골라 Wayback으로 이력을 육안 확정 ② 우량 라벨: 폐업한 유명 서비스·언론·기관의 만료 도메인(역사가 공개적으로 알려짐) ③ 경계 라벨: 장기 파킹·주제 전환 사례. 각 10±5개, 전부 "만료 상태" 필터로 파이프라인 전제와 분포 일치. 라벨은 사람이 최종 승인.
1. **골격** — FastAPI+화면 3개+SSE+캐시+시작 스크립트+localhost 바인딩 · *검증: 맥 실기+윈도(GitHub Actions 또는 가상머신)에서 시작 스크립트→UI까지, 차단 경고 대응 안내 검증 포함*
2. **이력·등록 수집기 [A][B]** · *검증: 장수·재등록·기록 없음·.kr 각 1개 이상 — 타임라인·등록 정보 사실 일치, 429 시 자동 하향 작동*
3. **평판·색인 [C][D]** · *검증: 시험지에서 등재/청정 분리, 파킹 도메인 색인 오탐 0*
4. **콘텐츠 분석 [E]** · *검증: 시험지 기준 — **탐지 가능 범위 내**(저장된 이력·색인·블랙리스트로 잡을 수 있는 유형) 미탐 0, 클로킹형 라벨은 게이트에서 제외하고 별도 보고. 카지노 '기사' 오탐이 신호 충돌 ⚠️로 빠지는지, 토큰 상한 준수*
5. **캡쳐+점수·판정+보고서 [F][G]** · *검증: 컷라인 75/50과 ⚠️ 비율(≤30%)을 시험지로 보정*
6. **끝판 검증** — 실제 낙장도메인 5~10개 전체 실행 → 눈 검수 + 시험지 정확도 수치 보고 → 완료 선언

## 7. 한계 (정직하게 — 보고서에도 표기)

- **클로킹·해킹 삽입형 스팸은 구조적으로 놓칠 수 있음** (규칙·AI가 같은 저장 HTML을 읽음). 색인 제목 검사가 부분 보완. → ✅도 "무위험 보증"이 아님을 보고서 상단에 명시
- 이력 없음 vs 스냅샷 차단(은폐 가능성)을 구분 표기 · 백링크 상세는 무료 API 부재(Q3 증거 얇음 명시) · 상표 검사는 보조 수준 · 색인은 site: 추정치 · 구매 가격·경매 시세 범위 밖 · 주인 교체 횟수 추적 안 함(요구 제외)

## 8. 사용자가 준비할 것
- (권장 — ✅ 판정에 필요) Serper.dev 키(카드 불필요) / Open PageRank 키
- (선택) Safe Browsing 키(절차 김), VirusTotal 키
- OpenRouter 키 — **기존 키는 지금 폐기·재발급**
