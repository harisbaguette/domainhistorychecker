# 낙장도메인 품질 체커 — 실행 계획서 (v5)

> 작성일: 2026-08-04 · 웹조사 확인일: 2026-08-04 · 저장소: git@github.com:harisbaguette/domainhistorychecker.git
> v5 = v4를 독립 심판이 적대 검증(결함 30건 발견)한 뒤 타당한 지적을 전부 반영한 판

## 목적 (모든 설계는 여기서 역산)

**"구글 상위노출용 사이트를 운영할, 안전하고 유리한 낙장도메인을 대량 후보 중에서 골라낸다."**

도메인마다 5개의 질문에 답하면 목적이 달성됨. 수단은 증거에, 증거는 질문에 종속 — 돈이 로직을 바꾸지 않는다:

| 판단 질문 | 필요한 증거 | 수단 |
|---|---|---|
| Q1. **위험한가?** | 전 기간 과거 콘텐츠의 스팸성 운영 여부, 블랙리스트, 색인 오염 | Wayback 전 기간 + AI 정독 + 색인 제목 검사 + Spamhaus·Safe Browsing |
| Q2. **건실한가?** | 운영 연수, 이력 흐름(전환의 방향), 리다이렉트·파킹 비중 | Wayback CDX + RDAP/whois + AI 주제 변천 |
| Q3. **물려받을 자산이 있는가?** | 권위 점수, 색인 잔존 | Open PageRank + Serper + (상세는 Ahrefs 무료 링크) |
| Q4. **뭘 하면 유리한가?** | 과거 주제·언어·독자층 → 이어갈 주제 | AI 추천 (구글 정책상 "과거 인접 주제 + 사람 우선"이 안전) |
| Q5. **지금 살 수 있나? 법적 문제는?** | 도메인 등록 상태, 상표 충돌 가능성 | RDAP status(삭제 대기/복원 기간/등록 중) + 브랜드명 검사 + AI 상표 충돌 소견 |

**안전 원칙 (심판 지적 반영)**: "증거 없음"은 "문제 없음"이 아님 — 어떤 검사든 실패·미설정이면 그 항목은 **미확인**으로 표시하고, 미확인이 하나라도 있으면 ✅ 판정을 발급하지 않음(최고 ⚠️).

## 참고: 구글 정책 (2026-08-04 조사)
- 공식 스팸 정책에 "만료도메인 남용" 항목 (2024-03 도입, 2026-05-15 갱신 문서에서 유효 확인): 금지 = 순위 조작 목적의 저가치 콘텐츠. 허용 = *"a new, original site that's designed to serve people first."* 적발 시 순위 하락·색인 제외.
- 업계 통설: 순위는 승계 안 됨, 자산은 백링크. 안전 조건 = 과거와 같거나 인접한 주제.
- 출처: developers.google.com/search/docs/essentials/spam-policies · name.com/blog · pbn.ltd · domraider.com

## 1. 파이프라인

```
브라우저 UI (목록 붙여넣기 → 정규화·중복 제거 → 규모 안내)
   │
   ├─[A] 이력 수집 — Wayback CDX 1회(타임라인: 연도 분포·공백·상태코드·차단 응답 구분)
   │      + 연도별 대표 스냅샷 본문 최대 6개(말기 필수), `id_` 원본, 본문 추출 후 스냅샷당 6천자
   ├─[B] 등록 정보 — RDAP → 실패 시 whois 폴백(.kr는 KRNIC whois, EUC-KR 인코딩 자동 감지)
   │      등록일·만료일 + **status로 취득 가능 상태**(삭제 대기/복원 기간/이전 잠금/등록 중)
   │      Wayback 첫 관측과 대조해 재등록(드랍) 이력 표시
   ├─[C] 평판 — Spamhaus DBL(시스템 리졸버, 차단 시 미확인) · Safe Browsing (·VirusTotal 옵션)
   ├─[D] 색인 — Serper site: 1쿼리: 색인 수 + 상위 제목. 제목 오염 검사는 "현재 파킹/재활용
   │      콘텐츠"를 과거 이력과 구분한 뒤 판단(현재 상태를 과거 탓으로 돌리는 오탐 방지)
   ├─[E] 콘텐츠 분석 — 병행 2갈래(같은 원천을 다른 눈으로 — 교차 검증):
   │      · 규칙: 명백 패턴 최소셋(도박·성인·약품 핵심 토큰 4개국어, 파킹 패턴, 언어 급변) → "후보 표시"만
   │      · AI: deepseek-v4-flash-0731 1회 — 주제 변천, **스팸성 운영 여부**(업종이 아니라
   │        도어웨이·대량 자동생성·숨김 링크 등 운영 방식 기준), 콘텐츠 품질, 전환 방향,
   │        상표 충돌 소견, 추천 운영 주제(한국어 출력, JSON 스키마 강제)
   │      · 도메인명 브랜드 검사: 유명 상표 문자열 포함 여부(사전) → 걸리면 ⚠️ 사유
   ├─[F] 캡쳐 — Playwright: 기본 말기 1장 + 위험 신호 시기 1장(있을 때만).
   │      Wayback 요청 예산에 계상, 검사 완료 후 후행 단계로 실행(표 먼저, 사진 나중)
   ▼
[G] 점수·판정·보고서 (코드)
```

- 규칙과 AI의 관계: 규칙은 후보 표시, 확정은 교차 — 둘이 일치하면 확정, 어긋나면 "신호 충돌 ⚠️"(예: 도박 낱말이 있는데 AI는 뉴스 기사로 판독 → 사람 확인).
- AI 입력 상한: 도메인당 본문 6천자×6개+통계 ≈ 4만자(약 1.5만 토큰) — 비용 산정 근거(§5).
- 캐시: 도메인별 결과 JSON — 중단·재개 버튼 지원(규모가 커도 나눠 돌림).

## 2. 점수·판정 (코드가 계산 — AI 출력은 입력값 중 하나)

**점수 (0~100)** — 배점 근거 포함:

| 항목 | 배점 | 근거·채점 방향 |
|---|---|---|
| 과거 콘텐츠 안전성 | 40 | Q1이 목적의 절반. AI 스팸성 판단 + 규칙 후보 + 색인 오염의 교차 결과 |
| 이력 건실성 | 20 | 연수·리다이렉트 남발(PBN 매도 흔적)·파킹 비중. 공백 단독은 소폭 감점(크롤 우연일 수 있음), **공백+직후 주제 급변 조합**만 위험 신호 |
| 전환 방향 | 15 | 일관성 자체가 아니라 방향: 정상→정상 전환은 소폭, 정상→위험 업종·언어 급변은 대폭 감점 |
| 승계 자산 | 15 | OPR 권위 + 색인 잔존은 **가점형** — 색인 0은 만료 도메인의 기본값이라 중립(감점 아님) |
| 현재 평판 | 10 | 만료 도메인은 대부분 무신호라 변별력 낮음 → 배점 작게. 등재는 점수 아닌 치명 신호로 처리 |

**판정 — 우선순위 순서대로 첫 번째 해당 규칙 적용 (충돌 불가능)**:

1. **치명 신호 → ❌ 거름**: 블랙리스트·Safe Browsing 등재(기관 확인) / 규칙+AI 이중 확인된 스팸성 운영 / 색인 오염(현재 파킹 콘텐츠 제외 후)
2. **점수 <50 → ❌ 거름**
3. **⚠️ 강제 사유 → ⚠️ 직접 확인**: 미확인 항목 존재 / 신호 충돌 / 상표 충돌 의심 / 합법이지만 민감 업종 이력(성인·의약 등 — 구글 상위노출 목적상 자체 리스크) / 스냅샷 차단(exclusion — 이력 은폐 가능성)
4. **50~74점 → ⚠️** / **75점+ → ✅ 사도 됨**
- 별도 열: **취득 상태** (지금 구매 가능 / 복원·경매 기간 / 등록 중 — 판정과 독립 표시. 가격·경매 시세는 범위 밖)
- 컷라인(75/50)·배점은 초기값 — §6의 라벨 세트 검증에서 실측 보정함을 명시

**보고서 (HTML)**: `index.html` 판정별 묶음+점수순(도메인/판정/점수/취득 상태/한줄평/추천 주제) → 상세: 나이·재등록, 타임라인, 주제 변천, 캡쳐, 걸린 신호+근거, 미확인 목록, 원자료, 재확인 링크(구글 site:·Ahrefs 무료 백링크·Moz DA·Whoisology·상표 검색). `results.json` 동봉.

## 3. 기술 스택 + 비개발자 설치 동선 (심판 지적 반영)

**Python 3.12 + FastAPI + 반응형 단일 HTML UI + httpx(비동기) + Playwright + uv**
- 근거: 전부 윈도우+맥 공식 지원 / 브라우저가 UI(프레임워크 0) / 모바일은 반응형 웹 그대로(추후 Tailscale 무료 — 2026-04 개편 6인·기기 무제한) / Tauri 탈락(모바일 Python sidecar 미지원, 공식 문서·GitHub #9774)

**설치·실행 동선 (비개발자 기준)**:
1. 저장소를 zip으로 받아 풀고, **더블클릭 한 번**: 맥 `시작.command` / 윈도 `시작.bat` — 스크립트가 uv·의존성·Playwright 브라우저(수백 MB, 최초 1회)를 자동 설치하고 브라우저를 열어 줌
2. 열쇠(API 키)는 `.env` 수기 작성이 아니라 **UI 설정 화면에서 입력·저장** (윈도우 확장자 함정 제거). 키가 없으면 해당 검사만 "미확인"으로 두고 나머지는 정상 실행
3. 설정 화면에 키 발급 안내 링크·단계별 그림 설명 포함 (Safe Browsing은 구글 클라우드 절차가 길어 "선택, 나중에" 허용)
4. AI 모델은 기본 `deepseek/deepseek-v4-flash-0731`, 설정에서 변경 가능 + 호출 실패 시 대체 모델(`-latest` → v3.2) 자동 폴백

⚠️ **보안 — 지금 즉시**: 채팅에 노출된 OpenRouter 키는 완성을 기다리지 말고 **바로 폐기·재발급**하고 새 키를 UI에 입력할 것. 키 파일은 `.gitignore`로 저장소에서 제외.

## 4. 속도 설계

- 도메인 사이 병렬(asyncio), 병목은 Wayback IP 제한뿐(분당 15~60건, 429 연타 시 1시간 차단 — 창 여러 개는 같은 IP라 무효, 2026-08-04 실측 조사): 전역 초당 0.5~1회 + 지수 백오프
- Wayback 예산(도메인당): CDX 1 + 본문 ≤6 + 캡쳐 1~2 ≈ 8~9건 → **100개 ≈ 15~25분** (캡쳐는 후행이라 표 결과는 더 일찍 나옴)
- 실행 시작 시 **예상 소요 시간·무료 쿼터 소진량을 미리 계산해 표시**, 500개 초과 시 분할 실행 안내
- VirusTotal(옵션, 분당 4건)은 별도 줄로 뒤따라옴
- (2차 확장) Common Crawl 본문 보충으로 대량 모드 가속 — 커버리지가 불확실해(대형 사이트 편향) MVP에서 제외, 실측 후 도입 판단

## 5. 비용표 (100개 기준, 2026-08-04 확인)

| 항목 | 비용 | 산정 근거 |
|---|---|---|
| AI (v4-flash-0731: 입력 $0.09/출력 $0.18 per 1M) | ≈ $0.2~0.5 | 도메인당 입력 ≈1.5만 토큰 + 출력 ≈1천 토큰 × 100 |
| Wayback·RDAP/whois·Spamhaus·Safe Browsing | 무료 | — |
| Serper | 무료 2,500쿼리 내 | 도메인당 1쿼리 설계(재시도 예비 ~10% 감안 시 실효 ~2,200개) |
| Open PageRank | 무료 | 월 3만, 100개 일괄 |
| VirusTotal (옵션) | 무료 | 500/일·4/분 |

## 6. 구현 순서 (검증 게이트 포함)

0. **정답 라벨 세트 구축** (심판 지적: 눈 검수만으로 미탐 측정 불가) — 알려진 위험 도메인(블랙리스트 등재·스팸 이력 공개 사례) 10개 + 알려진 우량(장수 운영 사이트) 10개 + 경계 사례 5~10개에 정답 라벨을 붙인 시험지 작성. 이후 모든 단계의 채점 기준.
1. **골격** — FastAPI+UI+SSE 진행률+설정 화면(키 입력)+캐시+시작 스크립트 · *검증: 맥·윈도(가상환경 또는 GitHub Actions)에서 더블클릭→UI까지*
2. **이력·등록 수집기 [A][B]** · *검증: 장수·재등록·기록 없음·.kr 도메인 각 1개 이상으로 타임라인·등록 정보가 사실과 일치, 429 없이 완주*
3. **평판·색인 [C][D]** · *검증: 라벨 세트에서 등재/청정이 갈리는지, 파킹 도메인이 색인 오탐을 안 내는지*
4. **콘텐츠 분석 [E]** · *검증: 라벨 세트 전체에서 미탐 0(위험인데 ✅ 없음)·오탐 확인, 카지노 '기사' 같은 사례가 신호 충돌 ⚠️로 빠지는지, AI 호출 토큰이 상한 내인지*
5. **캡쳐+점수·판정+보고서 [F][G]** · *검증: 컷라인 75/50을 라벨 세트로 보정*
6. **끝판 검증** — 실제 낙장도메인 5~10개 전체 실행 → 보고서 눈 검수 + 라벨 세트 정확도 수치 보고 → 완료 선언

## 7. 한계 (정직하게 — 보고서에도 표기)

- **클로킹·해킹 삽입형 스팸은 구조적으로 놓칠 수 있음**: 규칙과 AI 모두 같은 저장된 HTML을 읽으므로, 크롤러에게만 보였던 스팸·JS 주입 스팸은 안 남아 있으면 못 봄. 색인 제목 검사(구글이 실제로 본 모습)가 부분 보완이지만 완전하지 않음 → ✅ 판정도 "무위험 보증"이 아님을 보고서에 명시
- Wayback·색인 어디에도 기록 없는 도메인은 판단 불가 → "이력 없음(신중)", 스냅샷 차단(exclusion)은 별도로 "이력 은폐 가능성"으로 구분
- 백링크 상세는 무료 API 부재 → 권위 총점 + 수동 링크로 보완(Q3 증거가 얇음을 명시)
- 상표 검사는 보조 수준(문자열+AI 소견) — 확정은 사람이 상표 검색 링크로
- 색인은 site: 추정치, 구매 가격·경매 시세는 범위 밖, 주인 교체 횟수 추적 안 함(요구 제외)

## 8. 사용자가 준비할 것

- (권장) Serper.dev 키 — 카드 불필요 / Open PageRank 키
- (선택) Google Safe Browsing 키(절차 김), VirusTotal 키
- OpenRouter 키 — **기존 키는 지금 폐기·재발급** 후 새 키 사용
